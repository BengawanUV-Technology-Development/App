function ConfigView() {
  return (
    <section className="placeholder-stack">
      <header className="view-header">
        <div>
          <p className="view-kicker">Config</p>
          <h1 className="view-title">Parameter management</h1>
          <p className="view-copy">Quick presets and the full parameter table will land after the parameter API is added.</p>
        </div>
      </header>
      <article className="panel placeholder-card">
        <p className="panel-label">Phase 5 Placeholder</p>
        <h2>Config view is ready for grouped params</h2>
        <p className="muted">Next pieces: API service layer, parameter hook, grouped cards, and searchable table.</p>
      </article>
    </section>
  );
}

export default ConfigView;
