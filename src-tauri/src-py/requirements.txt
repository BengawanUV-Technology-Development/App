blinker==1.9.0
click==8.3.1
colorama==0.4.6
Flask==3.1.3
flask-cors==6.0.2
flask-sock==0.7.0
h11==0.16.0
itsdangerous==2.2.0
Jinja2==3.1.6
MarkupSafe==3.0.3
simple-websocket==1.1.0
typing_extensions==4.15.0
Werkzeug==3.1.8
wsproto==1.3.2
flasgger==0.9.7.1
google-genai==2.14.0
python-dotenv==1.2.2

# JPEG decode/MP4 recording and legacy EasyCAP support. Jetson UDP ingest uses
# the system GStreamer executable, not OpenCV's GStreamer backend.
opencv-python==4.12.0.88

# Ray-to-ground-plane target coordinate estimation (app/services/coordinate_estimator.py).
# Unpinned: opencv-python already pulls in a compatible numpy transitively,
# and pinning to a specific version here fights that resolution.
numpy>=1.26

# Jetson RTP/UDP video receiver (app/services/jetson_video_service.py) binds
# to GStreamer through GObject introspection ("import gi"). These wheels only
# link successfully once the underlying GLib/GObject-Introspection/GStreamer
# C libraries are present on the system:
#   Docker (Debian): already installed by this project's Dockerfile via apt.
#   macOS (manual/native run): run scripts/setup_macos_video_deps.sh first,
#     which installs the Homebrew formulas these wheels build against.
#   Jetson (jetson/*.py): uses the OS-provided python3-gi from apt; do not
#     pip-install PyGObject there, it would shadow the NVIDIA-tuned bindings.
pygobject==3.50.0
pycairo==1.27.0
